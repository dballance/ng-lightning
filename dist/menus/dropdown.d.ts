import { EventEmitter, ElementRef, OnInit, OnDestroy, QueryList, Renderer } from '@angular/core';
import { NglDropdownItem } from './dropdown-item';
import { NglPick } from '../pick/pick';
export declare class NglDropdown implements OnInit, OnDestroy {
    element: ElementRef;
    renderer: Renderer;
    private pick;
    isOpen: boolean | string;
    handlePageEvents: boolean;
    items: QueryList<NglDropdownItem>;
    isOpenChange: EventEmitter<boolean>;
    __isOpen: boolean | string;
    triggerFocusEventEmitter: EventEmitter<{}>;
    isPicklist: boolean;
    private handleGlobalClickEvents;
    private _isOpen;
    private openEventSubscription;
    private clickEventUnsubscriber;
    onKeydownClose(eventName: string): void;
    onKeydownFocusNext($event: Event, direction: 'next' | 'previous'): void;
    constructor(element: ElementRef, renderer: Renderer, pick: NglPick);
    ngOnInit(): void;
    ngOnDestroy(): void;
    toggle(toggle?: boolean, focus?: boolean): void;
    handleGlobalClickEvent($event: Event): void;
    private _subscribeToGlobalClickEvents();
    private _unsubscribeFromGlobalClickEvents();
    private focusItem(direction);
    private handleDropdownOpenEvent(dropdown);
}
